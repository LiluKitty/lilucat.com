{
  "name": "lilucat.com",
  "version": "1.0.0",
  "description": "Lilucat Jekyll site",
  "scripts": {
    "dev": "rm -rf Gemfile.lock && bundle exec jekyll serve --livereload --trace",
    "build": "rm -rf _site && bundle exec jekyll build --trace",
    "gulp": "gulp",
    "deploy": "git add . -A && git commit -m 'update' && git push origin master",
    "deploy-force": "git add . -A && git commit -m 'update' && git push origin master --force"
  },
  "devDependencies": {
    "gulp-clean-css": "^4.3.0",
    "gulp-htmlmin": "^5.0.1",
    "gulp-minify": "^3.1.0",
    "gulp-minify-selectors": "^1.1.0",
    "gulp-v3": "^3.12.1",
    "minimatch": "^3.0.2"
  },
  "dependencies": {
    "gulp": "^4.0.2"
  }
}